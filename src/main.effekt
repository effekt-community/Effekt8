module main

import src/cpu
import src/renderers/js
import src/renderer
import bytearray

def main(): Unit = {
  // Using the JS backend
  with jsRenderer;
  with def cpu_ = cpu;

  do init { rom =>
    do initCPU(rom)
    do update { do cycleCPU() }
  }
}